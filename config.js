var config = {};

config.period = 300;         // period of needed data in seconds, INFO --- 86400 - one DAY,  --- 3600 - one HOUR, --- 900 - 15 MIN
config.file_name = 'coinbase-2017-hd'; // enter your file name without extension (it must be at entry_data FOLDER)

module.exports = config;